name: TruffleHog
on:
  workflow_dispatch:
jobs:
  trufflehog:
    runs-on: ubuntu-latest
    steps:
      - uses: trufflesecurity/trufflehog@main
        with:
          scan_type: github
          org: Defra-Data-Science-Centre-of-Excellence
          github_token: ${{ secrets.TRUFFLEHOG_TOKEN }}
